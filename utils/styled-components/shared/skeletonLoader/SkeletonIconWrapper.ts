import styled from "styled-components"

const SkeletonIconWrapper = styled.div`
  svg {
    filter: invert(100%) sepia(75%) saturate(458%) hue-rotate(179deg)
      brightness(112%) contrast(84%);
  }
`

export default SkeletonIconWrapper
